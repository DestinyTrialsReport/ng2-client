export interface LBWeaponType {
  id: string;
  name: string;
  icon: string;
  kills: string;
  headshots: string;
  matches: string;
  tier: number;
  type: number;
}

export interface LBWeaponPercentage {
  name: string;
  icon: string;
  percentage: string;
  tier: number;
  bucketTypeHash: string;
}

