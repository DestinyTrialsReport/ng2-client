export interface LBWeaponType {
  name: string;
  icon: string;
  kills: string;
  headshots: string;
  matches: string;
  tier: number;
}

export interface LBWeaponPercentage {
  name: string;
  icon: string;
  percentage: string;
  tier: number;
  bucketTypeHash: string;
}

